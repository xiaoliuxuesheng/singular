# 7.4 删除子模块

- 删除原理 : 正常删除文件的方式顺序删除子模块

  1. 删除暂存区中的子模块

     ```sh
     git rm --cached 子模块文件夹
     ```

  2. 删除工作区子模块文件

     ```sh
     rm -rf  子模块文件夹
     ```

  3. 删除子模块的配置文件

     ```sh
     rm -rf .gitmodules
     ```

  4. 将本地库修改提交并push到远程库